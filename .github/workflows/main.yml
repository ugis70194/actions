on: [push]

jobs:
  component_auto_generetor:
    runs-on: ubuntu-latest
    name: auto gen
    steps:
      - name: check
        run: ls -al
      - name: component auto generetor
        id: generete
        uses: ugis70194/actions@v1.10
